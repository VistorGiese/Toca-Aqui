---
phase: 06-bug-fixes-ux
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - TocaAqui/screens/establishment/EstEditProfile.tsx
  - TocaAqui/navigation/EstablishmentNavigator.tsx
  - TocaAqui/screens/establishment/EstProfile.tsx
  - TocaAqui/http/establishmentService.ts
autonomous: true
requirements:
  - REQ-26

must_haves:
  truths:
    - "Tela EstEditProfile existe e pode ser navegada a partir de EstProfile"
    - "EstEditProfile carrega os dados atuais do perfil do estabelecimento"
    - "Usuário pode editar nome, descrição, tipo e salvar as alterações"
    - "EstEditProfile está registrada no EstStackParamList e no EstablishmentNavigator"
  artifacts:
    - path: "TocaAqui/screens/establishment/EstEditProfile.tsx"
      provides: "Tela de edição do perfil do estabelecimento"
      contains: "nome_estabelecimento"
    - path: "TocaAqui/navigation/EstablishmentNavigator.tsx"
      provides: "Rota EstEditProfile registrada"
      contains: "EstEditProfile"
    - path: "TocaAqui/screens/establishment/EstProfile.tsx"
      provides: "Botão editar perfil conectado a EstEditProfile"
      contains: "EstEditProfile"
  key_links:
    - from: "EstProfile botão editar"
      to: "EstEditProfile screen"
      via: "navigation.navigate('EstEditProfile')"
      pattern: "navigate.*EstEditProfile"
    - from: "EstEditProfile handleSalvar"
      to: "PATCH /estabelecimentos/:id"
      via: "api.patch"
      pattern: "api\\.patch.*estabelecimentos"
---

<objective>
Criar a tela `EstEditProfile` para edição do perfil do estabelecimento — inexistente no app — e conectá-la ao perfil do estabelecimento (EstProfile) e ao navigator.

Purpose: Sem esta tela, o estabelecimento não pode atualizar suas informações. Para o TCC, o perfil precisa mostrar dados reais e o dono precisa conseguir editá-los.
Output: Nova tela EstEditProfile com campos de edição funcionais, registrada no stack e acessível via botão em EstProfile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-bug-fixes-ux/06-CONTEXT.md

<interfaces>
<!-- Interfaces e contexto necessários -->

`EstStackParamList` (EstablishmentNavigator.tsx) — adicionar:
```typescript
export type EstStackParamList = {
  EstTabs: undefined;
  EstNewGig: { gigId?: number };
  EstGigApplications: { gigId: number; gigTitle: string };
  EstArtistProfile: { artistId: number };
  EstAcceptContract: { applicationId: number; artistId?: number; artistName: string; gigTitle: string; valorProposto?: number };
  EstShowDetail: { contractId: number };
  EstNotifications: undefined;
  EstRateArtist: { contractId: number; artistName: string; showDate: string };
  EstSettings: undefined;
  EstEditProfile: undefined;  // ADICIONAR
};
```

`establishmentService.getMyEstablishmentProfile` — já existe, retorna:
```typescript
export interface EstablishmentProfile {
  id: number;
  nome_estabelecimento: string;
  tipo_estabelecimento?: string;
  descricao?: string;
  generos_musicais?: string;
  horario_abertura?: string;
  horario_fechamento?: string;
  telefone_contato?: string;
  foto_url?: string;
  nota_media?: number;
  cidade?: string;
  estado?: string;
  capacidade?: number;
}
```

Para salvar alterações — verificar `EstablishmentRoutes.ts`:
- Provavelmente existe `PATCH /estabelecimentos/:id` para atualizar perfil
- Se não existir, usar `api.patch('/usuarios/perfil-estabelecimento', data)` ou endpoint similar

`EstProfile.tsx` — já tem função `goToSettings()` que navega para `EstSettings`:
```typescript
function goToSettings() {
  navigation.navigate("EstSettings");
}
```
Adicionar função similar para editar perfil:
```typescript
function goToEditProfile() {
  navigation.navigate("EstEditProfile");
}
```

Design system para EstEditProfile (consistente com EstProfile e EstSchedule):
```
DS.bg = "#09090F"
DS.surface = "#0F0B1E"
DS.card = "#13101F"
DS.border = "#1E1A30"
DS.accent = "#7B61FF"
DS.textPrimary = "#FFFFFF"
DS.textSecondary = "#8888AA"
Fontes: Montserrat-Bold, Montserrat-SemiBold, Montserrat-Regular

Input style:
  backgroundColor: "#13101F"
  borderRadius: 10
  borderWidth: 1
  borderColor: "#1E1A30"
  paddingHorizontal: 16
  paddingVertical: 14
  color: "#FFFFFF"
  fontFamily: "Montserrat-Regular"

Botão primário:
  backgroundColor: "#7B61FF"
  borderRadius: 12
  paddingVertical: 16
  alignItems: "center"
```

Tipos de estabelecimento para picker:
```typescript
const TIPOS = [
  { value: "bar", label: "Bar & Music Venue" },
  { value: "casa_show", label: "Casa de Show" },
  { value: "restaurante", label: "Restaurante" },
  { value: "club", label: "Club / Balada" },
  { value: "outro", label: "Espaço Cultural" },
];
```

`EstablishmentRoutes.ts` — verificar qual rota de update existe. Se existir `PUT /:id` ou `PATCH /:id`, usar diretamente. Obter o ID do estabelecimento de `AsyncStorage.getItem("estabelecimentoId")` ou de `establishmentService.getMyEstablishmentProfile()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar rota de update no backend e adicionar updateProfile no service</name>
  <files>TocaAqui/http/establishmentService.ts</files>
  <action>
    Ler `backend-TocaAqui/src/routes/EstablishmentRoutes.ts` para descobrir qual rota existe para atualizar o perfil do estabelecimento.

    Se existir `PATCH /estabelecimentos/:id` ou `PUT /estabelecimentos/:id`, adicionar em `establishmentService.ts`:
    ```typescript
    const updateMyEstablishmentProfile = async (id: number, data: Partial<EstablishmentProfile>): Promise<EstablishmentProfile> => {
      const r = await api.patch<EstablishmentProfile>(`/estabelecimentos/${id}`, data);
      return r.data;
    };
    ```

    Se não existir rota de update no EstablishmentRoutes, verificar `UserRoutes.ts` — pode existir `PATCH /usuarios/perfil-estabelecimento/:id`.

    Se nenhuma rota existir, criar em `EstablishmentRoutes.ts` uma rota simples:
    ```typescript
    router.patch('/:id', authMiddleware, async (req, res, next) => {
      try {
        const { id } = req.params;
        const { nome_estabelecimento, descricao, tipo_estabelecimento, telefone_contato } = req.body;
        const EstablishmentProfileModel = require('../models/EstablishmentProfileModel').default;
        const perfil = await EstablishmentProfileModel.findByPk(id);
        if (!perfil) { res.status(404).json({ error: 'Perfil não encontrado' }); return; }
        await perfil.update({ nome_estabelecimento, descricao, tipo_estabelecimento, telefone_contato });
        res.json(perfil);
      } catch (err) { next(err); }
    });
    ```

    Após definir o endpoint, adicionar `updateMyEstablishmentProfile` ao export do `establishmentService`:
    ```typescript
    export const establishmentService = {
      // ... existentes ...
      updateMyEstablishmentProfile,
    };
    ```
  </action>
  <verify>
    <automated>grep -n "updateMyEstablishmentProfile\|updateEstablishment" /c/Users/vitor/Documents/Dev/TCC\ -\ Toca\ Aqui/Toca-Aqui/TocaAqui/http/establishmentService.ts</automated>
  </verify>
  <done>
    `establishmentService` exporta `updateMyEstablishmentProfile`. Existe rota de update no backend (nova ou existente).
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar EstEditProfile.tsx e conectar ao navigator e EstProfile</name>
  <files>TocaAqui/screens/establishment/EstEditProfile.tsx, TocaAqui/navigation/EstablishmentNavigator.tsx, TocaAqui/screens/establishment/EstProfile.tsx</files>
  <action>
    **1. Criar `TocaAqui/screens/establishment/EstEditProfile.tsx`:**

    Tela completa com:
    - `useEffect` que carrega `establishmentService.getMyEstablishmentProfile()` e popula os campos
    - Estado: `nome`, `descricao`, `tipo`, `telefone`
    - Picker de tipo como chips clicáveis (TIPOS array definido acima)
    - Botão SALVAR que chama `establishmentService.updateMyEstablishmentProfile(profile.id, payload)`
    - Header com botão voltar e título "Editar Perfil"
    - ActivityIndicator durante loading e saving
    - Tratamento de erro com Alert

    Estrutura do componente:
    ```tsx
    import React, { useState, useEffect } from "react";
    import { View, Text, StyleSheet, ScrollView, TextInput, TouchableOpacity, ActivityIndicator, Alert } from "react-native";
    import { FontAwesome5 } from "@expo/vector-icons";
    import { useNavigation } from "@react-navigation/native";
    import { NativeStackNavigationProp } from "@react-navigation/native-stack";
    import AsyncStorage from "@react-native-async-storage/async-storage";
    import { EstStackParamList } from "@/navigation/EstablishmentNavigator";
    import { establishmentService } from "@/http/establishmentService";

    type NavProp = NativeStackNavigationProp<EstStackParamList>;

    const TIPOS = [
      { value: "bar", label: "Bar & Music Venue" },
      { value: "casa_show", label: "Casa de Show" },
      { value: "restaurante", label: "Restaurante" },
      { value: "club", label: "Club / Balada" },
      { value: "outro", label: "Espaço Cultural" },
    ];

    export default function EstEditProfile() {
      const navigation = useNavigation<NavProp>();
      const [loading, setLoading] = useState(true);
      const [saving, setSaving] = useState(false);
      const [profileId, setProfileId] = useState<number | null>(null);
      const [nome, setNome] = useState("");
      const [descricao, setDescricao] = useState("");
      const [tipo, setTipo] = useState("");
      const [telefone, setTelefone] = useState("");

      useEffect(() => {
        establishmentService.getMyEstablishmentProfile()
          .then(p => {
            setProfileId(p.id);
            setNome(p.nome_estabelecimento ?? "");
            setDescricao(p.descricao ?? "");
            setTipo(p.tipo_estabelecimento ?? "");
            setTelefone(p.telefone_contato ?? "");
          })
          .catch(() => Alert.alert("Erro", "Não foi possível carregar o perfil."))
          .finally(() => setLoading(false));
      }, []);

      const handleSalvar = async () => {
        if (!profileId) return;
        if (!nome.trim()) { Alert.alert("Atenção", "Informe o nome do estabelecimento."); return; }
        setSaving(true);
        try {
          await establishmentService.updateMyEstablishmentProfile(profileId, {
            nome_estabelecimento: nome.trim(),
            descricao: descricao.trim() || undefined,
            tipo_estabelecimento: tipo || undefined,
            telefone_contato: telefone.trim() || undefined,
          });
          Alert.alert("Sucesso", "Perfil atualizado com sucesso!");
          navigation.goBack();
        } catch (e: any) {
          Alert.alert("Erro", e?.response?.data?.message || "Não foi possível salvar.");
        } finally {
          setSaving(false);
        }
      };

      if (loading) {
        return (
          <View style={{ flex: 1, backgroundColor: "#09090F", justifyContent: "center", alignItems: "center" }}>
            <ActivityIndicator size="large" color="#7B61FF" />
          </View>
        );
      }

      return (
        <View style={s.root}>
          {/* Header */}
          <View style={s.header}>
            <TouchableOpacity onPress={() => navigation.goBack()} style={s.backBtn}>
              <FontAwesome5 name="chevron-left" size={14} color="#7B61FF" />
            </TouchableOpacity>
            <Text style={s.headerTitle}>Editar Perfil</Text>
            <View style={{ width: 34 }} />
          </View>

          <ScrollView contentContainerStyle={s.scroll} showsVerticalScrollIndicator={false} keyboardShouldPersistTaps="handled">
            {/* Nome */}
            <Text style={s.label}>NOME DO ESTABELECIMENTO</Text>
            <TextInput style={s.input} value={nome} onChangeText={setNome}
              placeholder="Nome do seu espaço" placeholderTextColor="#555577" />

            {/* Descrição */}
            <Text style={s.label}>DESCRIÇÃO</Text>
            <TextInput style={[s.input, { minHeight: 100, textAlignVertical: "top", paddingTop: 12 }]}
              value={descricao} onChangeText={setDescricao} multiline
              placeholder="Fale sobre o seu espaço, ambiente e público" placeholderTextColor="#555577" />

            {/* Tipo */}
            <Text style={s.label}>TIPO DE ESTABELECIMENTO</Text>
            <View style={s.chipRow}>
              {TIPOS.map(t => (
                <TouchableOpacity
                  key={t.value}
                  style={[s.chip, tipo === t.value && s.chipActive]}
                  onPress={() => setTipo(t.value)}
                  activeOpacity={0.7}
                >
                  <Text style={[s.chipText, tipo === t.value && s.chipTextActive]}>{t.label}</Text>
                </TouchableOpacity>
              ))}
            </View>

            {/* Telefone */}
            <Text style={s.label}>TELEFONE DE CONTATO</Text>
            <TextInput style={s.input} value={telefone} onChangeText={setTelefone}
              placeholder="(11) 99999-9999" placeholderTextColor="#555577"
              keyboardType="phone-pad" />

            {/* Botão salvar */}
            <TouchableOpacity style={[s.saveBtn, saving && { opacity: 0.6 }]}
              onPress={handleSalvar} disabled={saving} activeOpacity={0.85}>
              {saving
                ? <ActivityIndicator color="#FFFFFF" size="small" />
                : <Text style={s.saveBtnText}>SALVAR ALTERAÇÕES</Text>
              }
            </TouchableOpacity>
          </ScrollView>
        </View>
      );
    }

    const s = StyleSheet.create({
      root: { flex: 1, backgroundColor: "#09090F" },
      header: { flexDirection: "row", alignItems: "center", justifyContent: "space-between",
                paddingTop: 52, paddingHorizontal: 20, paddingBottom: 16 },
      backBtn: { width: 34, height: 34, borderRadius: 17, backgroundColor: "rgba(123,97,255,0.12)",
                 borderWidth: 1, borderColor: "#7B61FF", alignItems: "center", justifyContent: "center" },
      headerTitle: { fontFamily: "Montserrat-Bold", fontSize: 18, color: "#FFFFFF" },
      scroll: { paddingHorizontal: 20, paddingBottom: 40 },
      label: { fontFamily: "Montserrat-SemiBold", fontSize: 11, color: "#8888AA",
               letterSpacing: 2, marginBottom: 8, marginTop: 20 },
      input: { backgroundColor: "#13101F", borderRadius: 10, borderWidth: 1,
               borderColor: "#1E1A30", paddingHorizontal: 16, paddingVertical: 14,
               color: "#FFFFFF", fontFamily: "Montserrat-Regular", fontSize: 14 },
      chipRow: { flexDirection: "row", flexWrap: "wrap", gap: 8, marginTop: 4 },
      chip: { paddingHorizontal: 14, paddingVertical: 8, borderRadius: 20,
              borderWidth: 1.5, borderColor: "#1E1A30" },
      chipActive: { backgroundColor: "rgba(123,97,255,0.2)", borderColor: "#7B61FF" },
      chipText: { fontFamily: "Montserrat-SemiBold", fontSize: 12, color: "#8888AA" },
      chipTextActive: { color: "#7B61FF" },
      saveBtn: { backgroundColor: "#7B61FF", borderRadius: 12, paddingVertical: 16,
                 alignItems: "center", marginTop: 32 },
      saveBtnText: { fontFamily: "Montserrat-Bold", fontSize: 14, color: "#FFFFFF", letterSpacing: 1 },
    });
    ```

    **2. Atualizar `EstablishmentNavigator.tsx`:**
    - Importar `EstEditProfile` de `@/screens/establishment/EstEditProfile`
    - Adicionar `EstEditProfile: undefined` em `EstStackParamList`
    - Adicionar `<Stack.Screen name="EstEditProfile" component={EstEditProfile} />` no Stack.Navigator

    **3. Atualizar `EstProfile.tsx`:**
    Ler o arquivo `TocaAqui/screens/establishment/EstProfile.tsx`.
    Encontrar onde o botão de editar perfil está (pode ser um ícone de engrenagem ou um botão "Editar").
    Se não existir, adicionar um botão na seção de header/profile:
    ```tsx
    <TouchableOpacity onPress={() => navigation.navigate("EstEditProfile")} style={{ /* ícone editar */ }}>
      <FontAwesome5 name="edit" size={16} color={DS.accent} />
    </TouchableOpacity>
    ```
    Se já existir um botão de editar que não faz nada, conectá-lo a `navigation.navigate("EstEditProfile")`.
  </action>
  <verify>
    <automated>grep -rn "EstEditProfile" /c/Users/vitor/Documents/Dev/TCC\ -\ Toca\ Aqui/Toca-Aqui/TocaAqui/navigation/EstablishmentNavigator.tsx /c/Users/vitor/Documents/Dev/TCC\ -\ Toca\ Aqui/Toca-Aqui/TocaAqui/screens/establishment/EstProfile.tsx 2>/dev/null</automated>
  </verify>
  <done>
    - `EstEditProfile.tsx` criado com campos nome, descricao, tipo, telefone e botão salvar
    - `EstablishmentNavigator.tsx` registra EstEditProfile no stack
    - `EstProfile.tsx` tem botão editar que navega para EstEditProfile
  </done>
</task>

</tasks>

<verification>
1. Arquivo `TocaAqui/screens/establishment/EstEditProfile.tsx` existe
2. `grep "EstEditProfile" TocaAqui/navigation/EstablishmentNavigator.tsx` retorna import + Screen
3. `grep "EstEditProfile" TocaAqui/screens/establishment/EstProfile.tsx` retorna navigate call
4. `grep "nome_estabelecimento\|updateMyEstablishmentProfile" TocaAqui/screens/establishment/EstEditProfile.tsx` confirma campos e save
</verification>

<success_criteria>
- Tela EstEditProfile existe, carrega dados reais do perfil do estabelecimento
- Campos nome, descrição, tipo e telefone são editáveis e salvam via API
- Navigator registra a rota corretamente (sem erro de TypeScript em navigate)
- EstProfile tem botão que navega para EstEditProfile
- Visual consistente com o design system do estabelecimento (DS.accent = #7B61FF)
</success_criteria>

<output>
Após execução, criar `.planning/phases/06-bug-fixes-ux/06-04-SUMMARY.md`
</output>
