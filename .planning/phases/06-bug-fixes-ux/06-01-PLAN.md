---
phase: 06-bug-fixes-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TocaAqui/http/bandApplicationService.ts
  - TocaAqui/screens/establishment/EstGigs.tsx
  - TocaAqui/screens/artist/ArtistEPK.tsx
  - TocaAqui/screens/establishment/EstSettings.tsx
autonomous: true
requirements:
  - REQ-20
  - REQ-21
  - REQ-22

must_haves:
  truths:
    - "Abrir a aba VAGAS no perfil estabelecimento não crasha com v.toFixed is not a function"
    - "Artista consegue se candidatar a uma vaga e recebe confirmação (sem 404)"
    - "Botão Sair nos perfis artista e estabelecimento navega para a tela Login"
    - "Botão Sair no perfil comum (UserSettings) navega para Login (já usa signOut — verificar se funciona)"
  artifacts:
    - path: "TocaAqui/http/bandApplicationService.ts"
      provides: "Endpoint correto para candidatura de artista"
      contains: "api.post(\"/eventos\""
    - path: "TocaAqui/screens/establishment/EstGigs.tsx"
      provides: "Formatação segura de valores monetários"
      contains: "Number(v).toFixed"
    - path: "TocaAqui/screens/artist/ArtistEPK.tsx"
      provides: "Botão Sair funcional no perfil artista"
      contains: "signOut"
    - path: "TocaAqui/screens/establishment/EstSettings.tsx"
      provides: "Botão Sair funcional no perfil estabelecimento"
      contains: "signOut"
  key_links:
    - from: "TocaAqui/http/bandApplicationService.ts applyToEvent"
      to: "POST /eventos (BandApplicationRoutes)"
      via: "api.post(\"/eventos\", data)"
      pattern: "api\\.post.*\"/eventos\""
    - from: "ArtistEPK/EstSettings signOut button"
      to: "AuthContext.signOut()"
      via: "useAuth().signOut"
      pattern: "signOut"
---

<objective>
Corrigir os três bugs críticos encontrados no UAT que causam crashes ou bloqueiam o fluxo principal do TCC.

Purpose: Sem estas correções, o demo do TCC não consegue nem candidatar um artista nem visualizar vagas sem crashar. São pré-requisitos para qualquer outra demonstração.
Output: candidatura funcional sem 404, lista de vagas sem crash, logout operacional em todos os perfis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-bug-fixes-ux/06-CONTEXT.md

<interfaces>
<!-- Contexto crítico para o executor — leia antes de editar -->

Do backend `src/index.ts` — montagem das rotas:
```
app.use("/eventos", BandApplicationRoutes);   // BandApplicationRoutes montado em /eventos
```

Do `BandApplicationRoutes.ts`:
```typescript
router.post("/", validate(applyBandSchema), applyBandToEvent);  // POST /eventos
router.get("/minhas", getMyApplications);                        // GET /eventos/minhas
router.get("/:evento_id", getBandApplicationsForEvent);          // GET /eventos/:id
router.put("/:id/aceitar", checkEventOwnership, acceptBandApplication); // PUT /eventos/:id/aceitar
router.put("/:id/recusar", checkEventOwnership, rejectBandApplication); // PUT /eventos/:id/recusar
```

Do `bandApplicationService.ts` (BUG):
```typescript
// ERRADO — /candidaturas não existe no backend:
const response = await api.post<BandApplication>("/candidaturas", data);

// CORRETO deve ser:
const response = await api.post<BandApplication>("/eventos", data);
```

Do `EstGigs.tsx` (BUG):
```typescript
// ERRADO — toFixed() falha quando v é string:
function formatBRL(v?: number) { return v != null ? `R$ ${v.toFixed(2).replace(".",",")}` : "—"; }

// CORRETO:
function formatBRL(v?: number | string) {
  if (v == null) return "—";
  const n = Number(v);
  if (isNaN(n)) return "—";
  return `R$ ${n.toFixed(2).replace(".",",")}`;
}
```

Do `AuthContext.tsx` — signOut já implementado:
```typescript
const signOut = useCallback(async () => {
  isSigningOut.current = true;
  await userService.logout();           // POST /usuarios/logout + clear AsyncStorage
  await AsyncStorage.removeItem("userRole");
  setPaginas(null);
  clearAuth();                          // setUser(null) — dispara re-render para Login
  isSigningOut.current = false;
}, [clearAuth]);
```

Navigate.tsx usa `isAuthenticated: !!user` — quando user vira null, Navigator re-renderiza para telas de auth (Login). O signOut do AuthContext já funciona corretamente desde que seja chamado.

Do `ArtistEPK.tsx` (tela tab do artista):
- Usa `useAuth` → tem acesso a `signOut`
- Verificar se há botão "Sair" — se não existir, adicionar no header/rodapé
- NavigationProp é `RootStackParamList` (tela acessada via root stack)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir endpoint de candidatura (POST /candidaturas → POST /eventos)</name>
  <files>TocaAqui/http/bandApplicationService.ts</files>
  <action>
    Ler o arquivo completo `TocaAqui/http/bandApplicationService.ts`.

    O bug: `applyToEvent` chama `api.post("/candidaturas", data)` mas o backend monta BandApplicationRoutes em `/eventos` — portanto o endpoint correto é `POST /eventos`.

    Correção na função `applyToEvent`:
    - Trocar `api.post<BandApplication>("/candidaturas", data)` por `api.post<BandApplication>("/eventos", data)`

    Também verificar e corrigir se necessário:
    - `getMyApplications`: deve ser `GET /eventos/minhas` (já está correto)
    - `acceptApplication`: deve ser `PUT /eventos/:id/aceitar` (já está correto)
    - `rejectApplication`: deve ser `PUT /eventos/:id/recusar` (já está correto)

    Não alterar mais nada no arquivo.
  </action>
  <verify>
    <automated>grep -n "api.post" /c/Users/vitor/Documents/Dev/TCC\ -\ Toca\ Aqui/Toca-Aqui/TocaAqui/http/bandApplicationService.ts</automated>
  </verify>
  <done>A linha `applyToEvent` contém `api.post("/eventos", data)` e não `/candidaturas`.</done>
</task>

<task type="auto">
  <name>Task 2: Corrigir crash v.toFixed em EstGigs</name>
  <files>TocaAqui/screens/establishment/EstGigs.tsx</files>
  <action>
    Ler o arquivo `TocaAqui/screens/establishment/EstGigs.tsx`.

    O bug está na função `formatBRL` (linha ~15):
    ```typescript
    function formatBRL(v?: number) { return v != null ? `R$ ${v.toFixed(2).replace(".",",")}` : "—"; }
    ```

    O campo `cache_minimo` ou `preco_ingresso_inteira` pode chegar como string do backend. `.toFixed()` falha em strings.

    Substituir por:
    ```typescript
    function formatBRL(v?: number | string) {
      if (v == null) return "—";
      const n = Number(v);
      if (isNaN(n)) return "—";
      return `R$ ${n.toFixed(2).replace(".", ",")}`;
    }
    ```

    Não alterar mais nada no arquivo.
  </action>
  <verify>
    <automated>grep -n "formatBRL\|toFixed\|Number(" /c/Users/vitor/Documents/Dev/TCC\ -\ Toca\ Aqui/Toca-Aqui/TocaAqui/screens/establishment/EstGigs.tsx</automated>
  </verify>
  <done>`formatBRL` usa `Number(v)` antes de `.toFixed(2)`. A função aceita `number | string`.</done>
</task>

<task type="auto">
  <name>Task 3: Implementar logout no perfil artista (ArtistEPK) e criar/corrigir EstSettings</name>
  <files>TocaAqui/screens/artist/ArtistEPK.tsx, TocaAqui/screens/establishment/EstSettings.tsx</files>
  <action>
    **ArtistEPK.tsx:**
    Ler o arquivo completo `TocaAqui/screens/artist/ArtistEPK.tsx`.

    Verificar se existe botão "Sair" com `signOut`. Se não existir ou estiver vazio:
    1. Garantir que `useAuth` é importado e `signOut` é extraído do hook
    2. Adicionar função `handleSignOut`:
    ```typescript
    async function handleSignOut() {
      await signOut();
    }
    ```
    3. Adicionar botão ao final do ScrollView (antes do fechamento), seguindo o padrão visual do UserSettings:
    ```tsx
    <TouchableOpacity
      style={{ flexDirection: "row", alignItems: "center", justifyContent: "center",
               backgroundColor: "rgba(255,107,107,0.08)", borderWidth: 1,
               borderColor: "rgba(255,107,107,0.3)", borderRadius: 12,
               paddingVertical: 15, marginTop: 24, marginHorizontal: 20, marginBottom: 32 }}
      onPress={handleSignOut}
      activeOpacity={0.85}
    >
      <FontAwesome5 name="sign-out-alt" size={16} color="#FF6B6B" style={{ marginRight: 10 }} />
      <Text style={{ fontFamily: "Montserrat-Bold", fontSize: 14, color: "#FF6B6B", letterSpacing: 1.5 }}>
        SAIR DA CONTA
      </Text>
    </TouchableOpacity>
    ```

    **EstSettings.tsx:**
    Ler o arquivo `TocaAqui/screens/establishment/EstSettings.tsx`.

    Se o arquivo não existe, criar uma tela de configurações mínima com:
    - Header com título "Configurações"
    - Seção de conta mostrando nome/email do usuário (`useAuth()`)
    - Botão SAIR DA CONTA que chama `signOut()` com Alert de confirmação (igual ao UserSettings)
    - Estilo consistente com DS tokens: bg "#09090F", accent "#7B61FF", Montserrat

    Se o arquivo já existe mas não tem botão de logout funcional, adicionar botão seguindo o mesmo padrão do ArtistEPK acima.

    O componente deve ser exportado como default e usar `EstStackParamList` para tipagem de navegação se necessário.
  </action>
  <verify>
    <automated>grep -rn "signOut\|SAIR" /c/Users/vitor/Documents/Dev/TCC\ -\ Toca\ Aqui/Toca-Aqui/TocaAqui/screens/artist/ArtistEPK.tsx /c/Users/vitor/Documents/Dev/TCC\ -\ Toca\ Aqui/Toca-Aqui/TocaAqui/screens/establishment/EstSettings.tsx 2>/dev/null</automated>
  </verify>
  <done>
    - ArtistEPK.tsx contém botão "SAIR DA CONTA" que chama signOut()
    - EstSettings.tsx existe e contém botão "SAIR DA CONTA" que chama signOut()
  </done>
</task>

</tasks>

<verification>
Após execução, verificar:
1. `grep "api.post" TocaAqui/http/bandApplicationService.ts` deve mostrar `/eventos` (não `/candidaturas`)
2. `grep "Number(" TocaAqui/screens/establishment/EstGigs.tsx` deve mostrar `Number(v)` no formatBRL
3. `grep -l "signOut" TocaAqui/screens/artist/ArtistEPK.tsx TocaAqui/screens/establishment/EstSettings.tsx` deve listar ambos os arquivos
</verification>

<success_criteria>
- POST para candidatura não retorna 404 (endpoint alinhado com backend)
- Abrir lista de vagas em EstGigs não crasha com toFixed
- Perfil artista (ArtistEPK) tem botão Sair que chama signOut()
- EstSettings tem botão Sair que chama signOut()
- UserSettings já usa signOut() corretamente (sem alteração necessária)
</success_criteria>

<output>
Após execução, criar `.planning/phases/06-bug-fixes-ux/06-01-SUMMARY.md`
</output>
