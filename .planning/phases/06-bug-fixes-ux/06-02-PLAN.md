---
phase: 06-bug-fixes-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - TocaAqui/http/establishmentService.ts
  - TocaAqui/screens/user/UserSearch.tsx
autonomous: true
requirements:
  - REQ-23
  - REQ-24

must_haves:
  truths:
    - "Busca no perfil comum (UserSearch) retorna shows do backend ao digitar pelo menos 2 caracteres"
    - "Busca no perfil estabelecimento (EstSearch) retorna artistas sem erro 404"
  artifacts:
    - path: "TocaAqui/http/establishmentService.ts"
      provides: "Endpoint correto para busca de artistas"
      contains: "perfis-artista/busca"
    - path: "TocaAqui/screens/user/UserSearch.tsx"
      provides: "Busca funcional usando showService.searchShows"
      contains: "showService.searchShows"
  key_links:
    - from: "EstSearch → establishmentService.searchArtists"
      to: "GET /perfis-artista/busca ou endpoint existente"
      via: "api.get"
      pattern: "searchArtists"
    - from: "UserSearch → showService.searchShows"
      to: "GET /shows/buscar"
      via: "showService.searchShows"
      pattern: "searchShows"
---

<objective>
Tornar funcionais as buscas do perfil comum e do perfil estabelecimento, que atualmente retornam erro 404 ou não fazem chamada alguma ao backend.

Purpose: A busca no perfil comum é pré-requisito para o fluxo de descoberta de shows. A busca do estabelecimento é necessária para o demo de "encontrar artistas".
Output: UserSearch retorna shows reais; EstSearch busca artistas sem 404.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-bug-fixes-ux/06-CONTEXT.md

<interfaces>
<!-- Rotas backend existentes relevantes para busca -->

Backend `src/index.ts` — montagem:
```
app.use("/shows", ShowRoutes);
app.use("/artistas", ArtistaPublicoRoutes);
```

`ShowRoutes.ts`:
```typescript
router.get('/buscar', searchShows);    // GET /shows/buscar?q=...
router.get('/', getPublicShows);        // GET /shows
router.get('/destaque', getShowsDestaque);
router.get('/:id', getShowById);
```

`ArtistaPublicoRoutes.ts`:
```typescript
router.get('/seguindo', authMiddleware, getArtistasQueSigo);
router.get('/:id/publico', getPerfilPublico);   // GET /artistas/:id/publico
router.post('/:id/seguir', authMiddleware, seguirOuDesseguir);
// NÃO existe rota de busca em /artistas
```

`showService.ts` — já implementado e funcional:
```typescript
async searchShows(query: string, tipo?: "shows" | "artistas" | "locais"): Promise<any> {
  const response = await api.get("/shows/buscar", { params: { q: query, tipo } });
  return response.data;
}
```

`establishmentService.ts` — BUG (linha ~123):
```typescript
// ERRADO — /perfis-artista/busca não existe no backend:
const searchArtists = async (params?: { q?: string; genero?: string }): Promise<ArtistPublicProfile[]> => {
  const r = await api.get("/perfis-artista/busca", { params });
  return toArray<ArtistPublicProfile>(r.data);
};

// CORRETO — usar /artistas/:id/publico não serve para busca.
// A busca de shows com tipo="artistas" pode retornar artistas via ShowRoutes.
// Alternativa: GET /artistas/seguindo lista artistas seguidos (não é busca).
// SOLUÇÃO: Criar endpoint backend GET /artistas/busca OU usar /shows/buscar?tipo=artistas
// DECISÃO (Claude's Discretion): usar /shows/buscar?tipo=artistas como proxy — o ShowController.searchShows
// provavelmente aceita o parâmetro tipo. Se não funcionar, redirecionar para /agendamentos
// que lista artistas via candidaturas.
// ALTERNATIVA SEGURA: GET /usuarios/perfil-artista/busca se existir, senão criar
// rota simples GET /artistas/busca no backend ArtistaPublicoRoutes.
```

`UserSearch.tsx` — já implementado corretamente:
- Usa `showService.searchShows(q)` na função `fetchResults`
- Chama `GET /shows/buscar?q=...` — rota existe no backend
- O bug pode ser que `data.shows` está vindo como undefined — verificar a resposta
- `ShowRoutes.ts` tem `GET /buscar` → `searchShows` controller
- A resposta pode ser `{ shows: [...] }` ou diretamente um array

Investigar: se `showService.searchShows` já funciona e `UserSearch` já o chama, o bug pode ser no parsing da resposta (`data.shows ?? []` — se controller retorna `{ data: [...] }` em vez de `{ shows: [...] }`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir busca de artistas no EstSearch (endpoint 404)</name>
  <files>TocaAqui/http/establishmentService.ts</files>
  <action>
    Ler o arquivo completo `TocaAqui/http/establishmentService.ts`.

    O bug: `searchArtists` chama `GET /perfis-artista/busca` que não existe no backend.

    **Diagnóstico do backend:**
    - `/artistas` (ArtistaPublicoRoutes) não tem rota de busca por nome
    - `/shows/buscar?tipo=artistas` pode retornar artistas se o ShowController suportar o parâmetro `tipo`

    **Correção — adicionar rota de busca simples no backend e ajustar o service:**

    Primeiro, verificar se o ShowController aceita `tipo=artistas`. Se não, a solução mais simples é adicionar uma rota de busca no ArtistaPublicoRoutes no backend.

    **Estratégia (minimal blast radius):**
    Alterar `searchArtists` em `establishmentService.ts` para chamar `/artistas/busca` — e adicionar essa rota no backend em `ArtistaPublicoRoutes.ts`.

    Em `establishmentService.ts`, trocar:
    ```typescript
    const r = await api.get("/perfis-artista/busca", { params });
    ```
    por:
    ```typescript
    const r = await api.get("/artistas/busca", { params });
    ```

    Em seguida, adicionar no backend `backend-TocaAqui/src/routes/ArtistaPublicoRoutes.ts` uma nova rota ANTES das rotas com `/:id`:
    ```typescript
    // GET /artistas/busca?q=nome&genero=rock
    router.get('/busca', async (req, res, next) => {
      try {
        const { q, genero } = req.query as { q?: string; genero?: string };
        const { Op } = require('sequelize');
        const ArtistProfileModel = require('../models/ArtistProfileModel').default;
        const where: any = {};
        if (q) {
          where.nome_artistico = { [Op.like]: `%${q}%` };
        }
        if (genero) {
          where.generos_musicais = { [Op.like]: `%${genero}%` };
        }
        const artistas = await ArtistProfileModel.findAll({
          where,
          limit: 20,
          attributes: ['id', 'nome_artistico', 'tipo', 'generos_musicais', 'cache_minimo', 'cache_maximo', 'nota_media', 'shows_realizados', 'foto_url', 'cidade', 'estado'],
        });
        res.json(artistas);
      } catch (err) {
        next(err);
      }
    });
    ```

    Verificar como ArtistProfileModel é importado nesse arquivo e seguir o padrão de import existente. A rota `/busca` deve ser declarada ANTES de `/:id/publico` para não ser capturada pela rota paramétrica.

    Ao adicionar a rota, ajustar também o `toArray` em `establishmentService.ts` se necessário — o endpoint retorna array direto, então `toArray` deve funcionar.
  </action>
  <verify>
    <automated>grep -n "artistas/busca\|perfis-artista" /c/Users/vitor/Documents/Dev/TCC\ -\ Toca\ Aqui/Toca-Aqui/TocaAqui/http/establishmentService.ts</automated>
  </verify>
  <done>
    - `searchArtists` chama `/artistas/busca` (não `/perfis-artista/busca`)
    - `ArtistaPublicoRoutes.ts` tem rota `GET /busca` antes das rotas com `/:id`
  </done>
</task>

<task type="auto">
  <name>Task 2: Verificar e corrigir parsing da resposta em UserSearch</name>
  <files>TocaAqui/screens/user/UserSearch.tsx</files>
  <action>
    Ler o arquivo completo `TocaAqui/screens/user/UserSearch.tsx`.

    O fluxo atual:
    1. `fetchResults(q)` chama `showService.searchShows(q)`
    2. `showService.searchShows` faz `GET /shows/buscar?q=...`
    3. Resultado: `const shows: Show[] = data.shows ?? []`

    **Verificar o formato da resposta do backend:**
    O `ShowController.searchShows` pode retornar:
    - `{ shows: [...], total: N }` → `data.shows` funciona
    - `[...]` array direto → `data.shows` seria undefined → `[]` → nenhum resultado

    Se o backend retorna array direto ou um formato diferente de `{ shows: [...] }`, corrigir o parsing:
    ```typescript
    const fetchResults = useCallback(async (q: string) => {
      setLoading(true);
      try {
        const data = await showService.searchShows(q);
        // Suportar ambos os formatos: array direto ou { shows: [...] }
        const shows: Show[] = Array.isArray(data)
          ? data
          : (data?.shows ?? data?.data ?? data?.results ?? []);
        setResults(shows);
      } catch {
        Alert.alert("Erro", "Não foi possível buscar resultados.");
        setResults([]);
      } finally {
        setLoading(false);
      }
    }, []);
    ```

    Verificar também o `ShowController` para confirmar o formato de resposta. Ler `backend-TocaAqui/src/controllers/ShowController.ts` se necessário.

    Não alterar a UI — apenas o parsing da resposta. Se o parsing já estiver correto e a busca funcionar, fazer um smoke test mental e não alterar o arquivo.
  </action>
  <verify>
    <automated>grep -n "fetchResults\|data\.shows\|Array\.isArray" /c/Users/vitor/Documents/Dev/TCC\ -\ Toca\ Aqui/Toca-Aqui/TocaAqui/screens/user/UserSearch.tsx</automated>
  </verify>
  <done>
    `fetchResults` em `UserSearch.tsx` trata corretamente a resposta do backend — seja `{ shows: [] }` ou array direto.
  </done>
</task>

</tasks>

<verification>
1. `grep "artistas/busca" TocaAqui/http/establishmentService.ts` deve retornar a linha com a chamada correta
2. `grep "busca" backend-TocaAqui/src/routes/ArtistaPublicoRoutes.ts` deve mostrar a nova rota `/busca`
3. `grep "Array.isArray\|data\.shows" TocaAqui/screens/user/UserSearch.tsx` deve mostrar parsing robusto
</verification>

<success_criteria>
- EstSearch não retorna 404 ao buscar artistas (endpoint /artistas/busca existe no backend)
- UserSearch retorna shows ao digitar uma query de pelo menos 2 caracteres
- Nenhuma alteração visual nas telas de busca
</success_criteria>

<output>
Após execução, criar `.planning/phases/06-bug-fixes-ux/06-02-SUMMARY.md`
</output>
