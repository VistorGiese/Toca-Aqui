---
phase: 04-user-feed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TocaAqui/screens/user/UserFeed.tsx
  - TocaAqui/http/showService.ts
  - backend-TocaAqui/src/services/ShowService.ts
  - backend-TocaAqui/src/controllers/ShowController.ts
autonomous: true
requirements:
  - REQ-15

must_haves:
  truths:
    - "Feed exibe titulo_evento como título principal de cada card — nunca nome_banda"
    - "Filtro 'Hoje' envia esta_hoje=true ao backend e filtra shows do dia atual"
    - "Botão 'VER TODOS' sem onPress não aparece na tela"
    - "Seção abaixo dos cards reflete o filtro ativo (ex: 'Hoje' quando ativo é Hoje)"
    - "Feed exibe shows reais do banco com data, local e título"
  artifacts:
    - path: "TocaAqui/screens/user/UserFeed.tsx"
      provides: "Feed corrigido com título correto, filtro Hoje funcional, sem botão inútil"
    - path: "TocaAqui/http/showService.ts"
      provides: "ShowsParams com campo esta_hoje opcional"
    - path: "backend-TocaAqui/src/services/ShowService.ts"
      provides: "getPublicShows aceita e filtra por esta_hoje"
    - path: "backend-TocaAqui/src/controllers/ShowController.ts"
      provides: "getPublicShows lê esta_hoje do req.query e passa ao service"
  key_links:
    - from: "TocaAqui/screens/user/UserFeed.tsx"
      to: "TocaAqui/http/showService.ts"
      via: "showService.getPublicShows({ esta_hoje: true }) quando filtro é 'Hoje'"
    - from: "backend-TocaAqui/src/controllers/ShowController.ts"
      to: "backend-TocaAqui/src/services/ShowService.ts"
      via: "esta_hoje: esta_hoje === 'true' passado como param"
---

<objective>
Corrigir UserFeed para exibir dados reais com o comportamento correto: titulo_evento como título principal, filtro "Hoje" funcional end-to-end (frontend + backend), remoção do botão "VER TODOS" sem handler, e label da seção refletindo o filtro ativo.

Purpose: REQ-15 — usuário comum navega feed de eventos reais com dados corretos e filtros funcionais.
Output: UserFeed mostrando eventos reais do banco com título correto, filtro Hoje operacional e UI limpa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-user-feed/04-CONTEXT.md

<interfaces>
<!-- Interfaces relevantes extraídas do codebase. Executor usa direto — sem exploração. -->

De TocaAqui/http/showService.ts:
```typescript
export interface ShowsParams {
  cidade?: string;
  genero?: string;
  esta_semana?: boolean;
  fim_de_semana?: boolean;
  page?: number;
  limit?: number;
  // esta_hoje falta — será adicionado nesta task
}

export const showService = {
  async getPublicShows(params?: ShowsParams): Promise<ShowsResponse> {
    const response = await api.get<ShowsResponse>("/shows", { params });
    return response.data;
  },
  // ...
};
```

De backend-TocaAqui/src/controllers/ShowController.ts:
```typescript
export const getPublicShows = asyncHandler(async (req: Request, res: Response) => {
  const { cidade, genero, data_inicio, data_fim, esta_semana, fim_de_semana, page, limit } =
    req.query as Record<string, string>;
  const resultado = await showService.getPublicShows({
    // esta_hoje falta — será adicionado
    esta_semana: esta_semana === 'true',
    fim_de_semana: fim_de_semana === 'true',
    // ...
  });
  // ...
});
```

De backend-TocaAqui/src/services/ShowService.ts (getPublicShows):
```typescript
// Bloco de filtro de data — após o bloco fim_de_semana, antes do else final:
} else if (params.fim_de_semana) {
  // ... lógica sábado/domingo
} else {
  whereClause.data_show = { [Op.gte]: hoje };
}
// esta_hoje deve ser inserido como novo bloco entre fim_de_semana e o else
```

De TocaAqui/screens/user/UserFeed.tsx:
```typescript
// getShowTitle — COMPORTAMENTO ATUAL (errado):
function getShowTitle(show: Show): string {
  return show.Contract?.Band?.nome_banda || show.titulo_evento;
}
// DEVE ser: return show.titulo_evento;

// loadShows — trecho para adicionar Hoje:
const params: ShowsParams = {};
if (filter === "Esta semana") {
  params.esta_semana = true;
} else if (filter === "Fim de semana") {
  params.fim_de_semana = true;
}
// Adicionar: else if (filter === "Hoje") { params.esta_hoje = true; }

// Seção com label hardcoded "Esta semana":
<Text style={styles.sectionTitle}>Esta semana</Text>
// Deve ser dinâmico baseado em activeFilter

// Botão sem onPress (linhas 175-177):
<TouchableOpacity>
  <Text style={styles.seeAll}>VER TODOS</Text>
</TouchableOpacity>
// Remover este TouchableOpacity inteiro
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar esta_hoje ao backend (ShowService + ShowController)</name>
  <files>
    backend-TocaAqui/src/services/ShowService.ts,
    backend-TocaAqui/src/controllers/ShowController.ts
  </files>
  <read_first>
    backend-TocaAqui/src/services/ShowService.ts,
    backend-TocaAqui/src/controllers/ShowController.ts
  </read_first>
  <action>
    Em `ShowService.ts`, adicionar `esta_hoje?: boolean` na assinatura do params de `getPublicShows`. Inserir novo bloco `else if (params.esta_hoje)` entre o bloco `fim_de_semana` e o `else` final:

    ```typescript
    } else if (params.esta_hoje) {
      const fimHoje = new Date(hoje);
      fimHoje.setHours(23, 59, 59, 999);
      whereClause.data_show = { [Op.between]: [hoje, fimHoje] };
    } else {
    ```

    Em `ShowController.ts`, extrair `esta_hoje` do `req.query` junto com os demais campos e passá-lo ao service: `esta_hoje: esta_hoje === 'true'`. Nenhuma outra mudança no controller.
  </action>
  <verify>
    Iniciar o backend (`cd backend-TocaAqui && npx ts-node src/server.ts` ou equivalente) e fazer `curl "http://localhost:3000/shows?esta_hoje=true"` — deve retornar JSON com `shows` array (pode estar vazio se não houver shows hoje no banco, mas não deve retornar erro 500 nem shows fora do dia atual).
  </verify>
  <acceptance_criteria>
    Backend aceita `esta_hoje=true` como query param, filtra shows cujo `data_show` está entre 00:00:00 e 23:59:59 do dia atual, e retorna o array (vazio ou com dados) sem erro.
  </acceptance_criteria>
  <done>ShowService aceita esta_hoje, ShowController o lê e passa — sem erros de TypeScript</done>
</task>

<task type="auto">
  <name>Task 2: Corrigir UserFeed — título, filtro Hoje, label dinâmico, remover VER TODOS</name>
  <files>
    TocaAqui/screens/user/UserFeed.tsx,
    TocaAqui/http/showService.ts
  </files>
  <read_first>
    TocaAqui/screens/user/UserFeed.tsx,
    TocaAqui/http/showService.ts
  </read_first>
  <action>
    **1. showService.ts** — Adicionar `esta_hoje?: boolean` à interface `ShowsParams`. Nenhuma outra mudança.

    **2. UserFeed.tsx — getShowTitle (per decisão do usuário):**
    Alterar `getShowTitle` para retornar sempre `show.titulo_evento`:
    ```typescript
    function getShowTitle(show: Show): string {
      return show.titulo_evento;
    }
    ```

    **3. UserFeed.tsx — loadShows, bloco Hoje:**
    Após o bloco `fim_de_semana`, adicionar:
    ```typescript
    } else if (filter === "Hoje") {
      params.esta_hoje = true;
    }
    ```
    O filtro "Gratuitos" continua como client-side filter após o fetch (comportamento existente está correto).

    **4. UserFeed.tsx — remover botão "VER TODOS" (per decisão do usuário):**
    No bloco `<View style={styles.sectionHeader}>` da seção "Em Destaque", remover o `<TouchableOpacity>` com o texto "VER TODOS". Manter apenas o `<Text style={styles.sectionTitle}>Em Destaque</Text>`.

    **5. UserFeed.tsx — label dinâmico da seção de lista:**
    O `<Text style={styles.sectionTitle}>Esta semana</Text>` hardcoded deve usar o `activeFilter`:
    ```tsx
    <Text style={styles.sectionTitle}>{activeFilter}</Text>
    ```
    O subtítulo "Eventos acontecendo perto de você" pode ser mantido como está.

    **6. UserFeed.tsx — empty state para Hoje:**
    No bloco de lista vazia (`listShows.length === 0`), manter a mensagem existente "Nenhum show encontrado" — é genérico suficiente para todos os filtros.

    Não alterar: useEffect, useCallback, loadFeatured, estilos, navegação, toggleFavorite.
  </action>
  <verify>
    `npx tsc --noEmit` no diretório TocaAqui sem erros de tipo. Confirmar visualmente que: (1) ao selecionar "Hoje", o app chama `/shows?esta_hoje=true` no network; (2) títulos dos cards mostram `titulo_evento`, não `nome_banda`; (3) não há botão "VER TODOS" na seção Em Destaque.
  </verify>
  <acceptance_criteria>
    - `ShowsParams` tem `esta_hoje?: boolean`
    - `getShowTitle` retorna `show.titulo_evento` (sem fallback para nome_banda)
    - Filtro "Hoje" envia `esta_hoje: true` para o backend
    - Botão "VER TODOS" removido da seção Em Destaque
    - Label da seção de lista exibe `activeFilter` dinamicamente
    - Zero erros de TypeScript
  </acceptance_criteria>
  <done>UserFeed corrigido com título, filtro, label dinâmico e sem botão inútil</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` em TocaAqui — zero erros
2. `npx tsc --noEmit` em backend-TocaAqui — zero erros
3. Feed exibe `titulo_evento` em todos os cards (verificar card de show que tem contrato — não deve aparecer nome_banda)
4. Selecionar filtro "Hoje" — observar request de rede com `esta_hoje=true`
5. Seção abaixo do destaque mostra o nome do filtro ativo (ex: "Hoje", "Esta semana")
6. Seção "Em Destaque" não tem botão "VER TODOS"
</verification>

<success_criteria>
- Feed exibe eventos reais do banco com `titulo_evento` como título
- Filtro "Hoje" funciona end-to-end: frontend envia param, backend filtra por dia atual
- UI sem botão sem handler
- Sem crashes ao navegar no feed
</success_criteria>

<output>
After completion, create `.planning/phases/04-user-feed/04-01-SUMMARY.md` with what was done, files modified, and key decisions.
</output>
