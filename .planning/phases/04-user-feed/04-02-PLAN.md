---
phase: 04-user-feed
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - TocaAqui/screens/user/UserShowDetail.tsx
autonomous: true
requirements:
  - REQ-16

must_haves:
  truths:
    - "Seção 'O Artista' exibe nome e gêneros do artista quando show tem contrato aceito com Band"
    - "Seção 'O Artista' exibe 'Artista a ser confirmado' quando show não tem contrato aceito"
    - "Botão 'ver perfil completo' e navegação para UserArtistProfile estão desabilitados quando não há artista confirmado"
    - "Título da tela exibe show.titulo_evento — nunca nome_banda"
    - "Navegação Feed → UserShowDetail → goBack funciona sem crash"
  artifacts:
    - path: "TocaAqui/screens/user/UserShowDetail.tsx"
      provides: "UserShowDetail com seção artista condicional e navegação segura"
  key_links:
    - from: "TocaAqui/screens/user/UserShowDetail.tsx"
      to: "TocaAqui/http/showService.ts"
      via: "showService.getShowById(showId) — Show.Contract?.Band lido condicionalmente"
    - from: "TocaAqui/screens/user/UserShowDetail.tsx"
      to: "UserArtistProfile"
      via: "navigation.navigate('UserArtistProfile', { artistId }) — só quando show.Contract?.Band?.id existe"
---

<objective>
Corrigir UserShowDetail para exibir o artista confirmado condicionalmente: quando há contrato aceito com Band, exibe nome e gêneros; quando não há, exibe "Artista a ser confirmado" sem permitir navegação ao perfil. Eliminar o fallback `artistId ?? 1` que causa navegação inválida.

Purpose: REQ-16 — usuário comum vê detalhe completo do evento com artista confirmado (quando houver).
Output: UserShowDetail com seção artista correta, sem crashes, sem navegação para perfil inexistente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-user-feed/04-CONTEXT.md

<interfaces>
<!-- Interfaces extraídas do codebase. Executor usa direto — sem exploração. -->

De TocaAqui/http/showService.ts:
```typescript
export interface Show {
  id: number;
  titulo_evento: string;
  descricao_evento?: string;
  data_show: string;
  horario_inicio: string;
  horario_fim: string;
  genero_musical?: string;
  preco_ingresso_inteira?: number;
  capacidade_maxima?: number;
  ingressos_vendidos?: number;
  classificacao_etaria?: string;
  esta_publico: boolean;
  EstablishmentProfile?: {
    id: number;
    nome_estabelecimento: string;
    tipo_estabelecimento: string;
    telefone_contato?: string;
    Address?: { cidade: string; estado: string; rua: string; numero: string; bairro: string };
  };
  Contract?: {
    Band?: { id: number; nome_banda: string; generos_musicais?: string[] };
  };
}
```

De TocaAqui/screens/user/UserShowDetail.tsx (trechos problemáticos):
```typescript
// Linha 98-104 — cálculos derivados problemáticos:
const artistName = show.Contract?.Band?.nome_banda || show.titulo_evento; // errado: usa titulo_evento como fallback
const artistId = show.Contract?.Band?.id ?? 1;  // errado: fallback 1 causa navegação inválida

function goToArtist() {
  navigation.navigate("UserArtistProfile", { artistId }); // navega sempre, mesmo sem artista
}

// Linhas 233-252 — seção O Artista (sempre renderiza como clicável):
<TouchableOpacity style={styles.artistCard} onPress={goToArtist} activeOpacity={0.85}>
  <View style={styles.artistAvatar}>...</View>
  <View style={styles.artistInfo}>
    <Text style={styles.artistName}>{artistName}</Text>
    {show.Contract?.Band?.generos_musicais?.length ? (
      <Text style={styles.artistBio}>{show.Contract.Band.generos_musicais.join(", ")}</Text>
    ) : null}
  </View>
  <TouchableOpacity style={styles.viewProfileBtn} onPress={goToArtist}>
    <Text style={styles.viewProfileText}>ver perfil completo</Text>
    <FontAwesome5 name="chevron-right" size={10} color="#A78BFA" />
  </TouchableOpacity>
</TouchableOpacity>

// Linha 143 — título da cover usa titulo_evento (correto — não alterar):
<Text style={styles.coverTitle}>{show.titulo_evento}</Text>
```

De TocaAqui/navigation/UserNavigator.tsx (tipo de param):
```typescript
// UserStackParamList deve ter:
UserArtistProfile: { artistId: number };
// goToArtist só pode ser chamado quando artistId é um number válido
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir seção "O Artista" — exibição condicional e navegação segura</name>
  <files>TocaAqui/screens/user/UserShowDetail.tsx</files>
  <read_first>TocaAqui/screens/user/UserShowDetail.tsx</read_first>
  <action>
    Há dois problemas distintos a corrigir nesta tela:

    **1. Derivação de artistName e artistId (per decisão do usuário):**

    Substituir as linhas:
    ```typescript
    const artistName = show.Contract?.Band?.nome_banda || show.titulo_evento;
    const artistId = show.Contract?.Band?.id ?? 1;
    ```
    Por:
    ```typescript
    const confirmedBand = show.Contract?.Band ?? null;
    const artistId = confirmedBand?.id ?? null; // null quando sem artista confirmado
    ```

    **2. Função goToArtist — guardar contra artistId nulo:**
    ```typescript
    function goToArtist() {
      if (!artistId) return; // guard: nunca navegar sem artista real
      navigation.navigate("UserArtistProfile", { artistId });
    }
    ```

    **3. Seção "O Artista" — renderização condicional (per decisão do usuário):**

    Substituir todo o bloco `<View style={styles.section}>` que contém "O Artista" por lógica condicional:

    - Quando `confirmedBand` existe: renderizar o card como antes, com nome_banda, generos_musicais, e o botão "ver perfil completo" clicável.
    - Quando `confirmedBand` é null: renderizar o card com estado neutro — sem onPress no TouchableOpacity externo (usar `<View>` em vez de `<TouchableOpacity>`), exibir texto "Artista a ser confirmado" em vez do nome, omitir gêneros e omitir o botão "ver perfil completo".

    Implementação sugerida para o card sem artista confirmado:
    ```tsx
    <View style={styles.section}>
      <Text style={styles.sectionTitle}>O Artista</Text>
      {confirmedBand ? (
        <TouchableOpacity style={styles.artistCard} onPress={goToArtist} activeOpacity={0.85}>
          <View style={styles.artistAvatar}>
            <FontAwesome5 name="microphone" size={20} color="#A78BFA" />
          </View>
          <View style={styles.artistInfo}>
            <Text style={styles.artistName}>{confirmedBand.nome_banda}</Text>
            {confirmedBand.generos_musicais?.length ? (
              <Text style={styles.artistBio} numberOfLines={2}>
                {confirmedBand.generos_musicais.join(", ")}
              </Text>
            ) : null}
          </View>
          <TouchableOpacity style={styles.viewProfileBtn} onPress={goToArtist}>
            <Text style={styles.viewProfileText}>ver perfil completo</Text>
            <FontAwesome5 name="chevron-right" size={10} color="#A78BFA" />
          </TouchableOpacity>
        </TouchableOpacity>
      ) : (
        <View style={styles.artistCard}>
          <View style={styles.artistAvatar}>
            <FontAwesome5 name="microphone" size={20} color="#555577" />
          </View>
          <View style={styles.artistInfo}>
            <Text style={[styles.artistName, { color: "#555577" }]}>
              Artista a ser confirmado
            </Text>
          </View>
        </View>
      )}
    </View>
    ```

    **Não alterar:** título da cover (`show.titulo_evento`), `goToCheckout`, `goToComments`, loading state, estilos, seção "O Local", sticky bottom bar.
  </action>
  <verify>
    `npx tsc --noEmit` em TocaAqui — zero erros. Abrir um show sem contrato aceito: seção "O Artista" exibe "Artista a ser confirmado" em cinza, sem botão de perfil. Abrir um show com contrato aceito: seção exibe nome_banda, gêneros e botão "ver perfil completo" clicável. Pressionar goBack retorna ao Feed sem crash.
  </verify>
  <acceptance_criteria>
    - `artistId` é `null` quando `show.Contract?.Band` é undefined/null — nunca usa fallback 1
    - `goToArtist` tem guard `if (!artistId) return` — nunca navega para artistId inválido
    - Sem artista confirmado: card exibe "Artista a ser confirmado" como `<View>` não clicável
    - Com artista confirmado: card exibe nome_banda, gêneros, botão "ver perfil completo"
    - Zero erros de TypeScript
  </acceptance_criteria>
  <done>UserShowDetail exibe artista confirmado condicionalmente sem crashes nem navegação inválida</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` em TocaAqui — zero erros
2. Abrir show sem contrato aceito: seção "O Artista" mostra "Artista a ser confirmado" em cinza; sem botão de perfil; toque no card não faz nada
3. Abrir show com contrato aceito: seção mostra nome_banda e gêneros; botão "ver perfil completo" navega para UserArtistProfile
4. `navigation.goBack()` do detalhe retorna ao feed sem crash
5. Título na cover sempre exibe `titulo_evento` (não nome_banda)
</verification>

<success_criteria>
- Usuário abre detalhe e vê título, data, local e artista confirmado (quando houver)
- Sem navegação inválida para artistId=1 quando artista não está confirmado
- Navegação Feed → UserShowDetail → goBack funciona sem erros
</success_criteria>

<output>
After completion, create `.planning/phases/04-user-feed/04-02-SUMMARY.md` with what was done, files modified, and key decisions.
</output>
