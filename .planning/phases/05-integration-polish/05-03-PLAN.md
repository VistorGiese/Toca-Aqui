---
phase: 05-integration-polish
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - TocaAqui/screens/establishment/EstSchedule.tsx
  - TocaAqui/screens/establishment/EstGigApplications.tsx
  - TocaAqui/screens/artist/ArtistSchedule.tsx
  - TocaAqui/screens/artist/MyApplications.tsx
  - TocaAqui/screens/artist/ContractDetail.tsx
  - TocaAqui/screens/user/UserFeed.tsx
  - TocaAqui/screens/user/UserShowDetail.tsx
autonomous: false
requirements: [REQ-17]
must_haves:
  truths:
    - "All main flow screens use consistent ActivityIndicator color #A78BFA during loading"
    - "No buttons in the main TCC flow have empty onPress handlers"
    - "Demo ponta-a-ponta works: criar evento -> candidatar -> aceitar -> contrato -> confirmar -> marcar realizado -> avaliar -> feed"
  artifacts:
    - path: "TocaAqui/screens/establishment/EstSchedule.tsx"
      provides: "Loading state with #A78BFA"
      contains: "#A78BFA"
    - path: "TocaAqui/screens/establishment/EstGigApplications.tsx"
      provides: "Loading state with #A78BFA"
      contains: "#A78BFA"
    - path: "TocaAqui/screens/artist/ArtistSchedule.tsx"
      provides: "Loading state with #A78BFA"
      contains: "#A78BFA"
  key_links: []
---

<objective>
Audit and standardize loading states across all main flow screens, and audit onPress handlers for empty/noop buttons.

Purpose: Per D-08/D-10/D-11, ensure visual consistency in loading states and remove any non-functional buttons from the TCC demo flow. This is the final polish pass before the demo.

Output: All main flow screens use ActivityIndicator color="#A78BFA", no empty onPress handlers remain.
</objective>

<execution_context>
@.planning/phases/05-integration-polish/05-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize ActivityIndicator color to #A78BFA in all main flow screens</name>
  <files>TocaAqui/screens/establishment/EstSchedule.tsx, TocaAqui/screens/establishment/EstGigApplications.tsx, TocaAqui/screens/artist/ArtistSchedule.tsx, TocaAqui/screens/artist/MyApplications.tsx, TocaAqui/screens/artist/ContractDetail.tsx</files>
  <read_first>
    - TocaAqui/screens/establishment/EstSchedule.tsx (line ~122, ActivityIndicator color)
    - TocaAqui/screens/establishment/EstGigApplications.tsx (line ~95, ActivityIndicator color)
    - TocaAqui/screens/artist/ArtistSchedule.tsx (line ~150, ActivityIndicator color)
    - TocaAqui/screens/artist/MyApplications.tsx (line ~83, ActivityIndicator color)
    - TocaAqui/screens/artist/ContractDetail.tsx (line ~126, ActivityIndicator color)
  </read_first>
  <action>
Per D-10, the established pattern is `ActivityIndicator color="#A78BFA"`. Several screens use `DS.accent` (which is `#7B61FF`) instead.

For each of these screens, find the loading-state `ActivityIndicator` and change its `color` prop to `"#A78BFA"`:

1. **EstSchedule.tsx** (line ~122): Change `color={DS.accent}` to `color="#A78BFA"`
2. **EstGigApplications.tsx** (line ~95): Change `color={DS.accent}` to `color="#A78BFA"`
3. **ArtistSchedule.tsx** (line ~150): Change `color={DS.accent}` to `color="#A78BFA"`
4. **MyApplications.tsx** (line ~83): Change `color={DS.accent}` to `color="#A78BFA"`
5. **ContractDetail.tsx** (line ~126): Change `color={DS.accent}` to `color="#A78BFA"`

Do NOT change ActivityIndicator colors used inside buttons (e.g., submitting states) -- only the full-screen loading indicators.

Screens that already use `#A78BFA` (UserFeed, UserShowDetail) need no changes.
  </action>
  <verify>
    <automated>cd "C:/Users/vitor/Documents/Dev/TCC - Toca Aqui/Toca-Aqui" && for f in TocaAqui/screens/establishment/EstSchedule.tsx TocaAqui/screens/establishment/EstGigApplications.tsx TocaAqui/screens/artist/ArtistSchedule.tsx TocaAqui/screens/artist/MyApplications.tsx TocaAqui/screens/artist/ContractDetail.tsx; do echo "=== $f ===" && grep "ActivityIndicator.*color" "$f"; done</automated>
  </verify>
  <acceptance_criteria>
    - EstSchedule.tsx loading ActivityIndicator contains `color="#A78BFA"`
    - EstGigApplications.tsx loading ActivityIndicator contains `color="#A78BFA"`
    - ArtistSchedule.tsx loading ActivityIndicator contains `color="#A78BFA"`
    - MyApplications.tsx loading ActivityIndicator contains `color="#A78BFA"`
    - ContractDetail.tsx loading ActivityIndicator contains `color="#A78BFA"`
    - UserFeed.tsx already has `color="#A78BFA"` (no change needed, just verify)
    - UserShowDetail.tsx already has `color="#A78BFA"` (no change needed, just verify)
  </acceptance_criteria>
  <done>All 7 main flow screens use ActivityIndicator color="#A78BFA" for their loading state</done>
</task>

<task type="auto">
  <name>Task 2: Audit onPress handlers in main flow screens</name>
  <files>TocaAqui/screens/establishment/EstGigApplications.tsx, TocaAqui/screens/establishment/EstSchedule.tsx, TocaAqui/screens/establishment/EstShowDetail.tsx, TocaAqui/screens/artist/ArtistSchedule.tsx, TocaAqui/screens/artist/ArtistHome.tsx, TocaAqui/screens/artist/MyApplications.tsx, TocaAqui/screens/artist/ContractDetail.tsx, TocaAqui/screens/user/UserFeed.tsx, TocaAqui/screens/user/UserShowDetail.tsx</files>
  <read_first>
    - TocaAqui/screens/establishment/EstGigApplications.tsx (all onPress handlers)
    - TocaAqui/screens/establishment/EstSchedule.tsx (all onPress handlers)
    - TocaAqui/screens/establishment/EstShowDetail.tsx (all onPress handlers)
    - TocaAqui/screens/artist/ArtistSchedule.tsx (all onPress handlers)
    - TocaAqui/screens/artist/ArtistHome.tsx (all onPress handlers)
    - TocaAqui/screens/artist/MyApplications.tsx (all onPress handlers)
    - TocaAqui/screens/artist/ContractDetail.tsx (all onPress handlers)
    - TocaAqui/screens/user/UserFeed.tsx (all onPress handlers)
    - TocaAqui/screens/user/UserShowDetail.tsx (all onPress handlers)
  </read_first>
  <action>
Per D-08 and D-09, audit all 9 priority screens for buttons with empty onPress handlers (`onPress={() => {}}`) or handlers that do nothing meaningful (e.g., console.log only, alert "em breve", etc.).

For each screen listed in D-09:
1. Read the file and search for all `onPress` props
2. Check if any handler is empty (`() => {}`), a noop, or just logs
3. For any non-functional button found:
   - If the button is irrelevant to the TCC demo flow: replace `TouchableOpacity` with `View` (remove the interactive element entirely) per D-08
   - If the button should navigate somewhere logical: add the correct navigation
   - Do NOT use `Alert.alert("Em breve")` pattern -- clean removal is preferred per D-08

Initial grep showed NO instances of `onPress={() => {}}` across the codebase, so this task is primarily a verification pass. If all handlers are functional, document that in the summary and make no changes.

Per D-07: Do NOT modify buttons that navigate to `UserCheckout` -- those are kept as-is.
  </action>
  <verify>
    <automated>cd "C:/Users/vitor/Documents/Dev/TCC - Toca Aqui/Toca-Aqui" && grep -rn "onPress={() => {}}" TocaAqui/screens/establishment/ TocaAqui/screens/artist/ TocaAqui/screens/user/UserFeed.tsx TocaAqui/screens/user/UserShowDetail.tsx || echo "No empty onPress handlers found"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `onPress={() => {}}` in all 9 priority screens returns no matches
    - No TouchableOpacity exists without a meaningful onPress in the 9 priority screens
    - UserCheckout navigation buttons remain untouched per D-07
  </acceptance_criteria>
  <done>All buttons in the main TCC flow have functional handlers or have been removed/replaced with non-interactive Views</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end demo verification</name>
  <what-built>Complete integration polish: contract completion flow, artist rating flow, standardized loading states, clean onPress handlers</what-built>
  <how-to-verify>
    Run the full TCC demo flow:
    1. Login as establishment -> go to EstSchedule -> tap a confirmed show (status "aceito")
    2. In EstShowDetail, verify "MARCAR COMO REALIZADO" green button is visible
    3. Tap "MARCAR COMO REALIZADO" -> confirm in Alert -> verify status changes to "REALIZADO" and "AVALIAR ARTISTA" button appears
    4. Tap "AVALIAR ARTISTA" -> rate with stars + optional comment -> tap "ENVIAR AVALIACAO"
    5. Verify success Alert appears and navigation returns to EstShowDetail
    6. Login as artist -> check profile -> verify shows_realizados incremented
    7. Verify all listing screens show purple ActivityIndicator (#A78BFA) during loading
    8. Navigate through main flow screens and verify no buttons appear non-functional
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. All screens use consistent #A78BFA loading color
2. No empty onPress handlers in main flow screens
3. Full demo flow works without visible errors
</verification>

<success_criteria>
- All 7 main flow screens use ActivityIndicator color="#A78BFA"
- Zero empty onPress handlers in 9 priority screens
- Demo: create event -> apply -> accept -> contract -> confirm -> mark complete -> rate artist -> feed all works
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-polish/05-03-SUMMARY.md`
</output>
